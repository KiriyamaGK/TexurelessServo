{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Debug with Conda (cns)",
            "type": "python",
            "env": {
                "PYTHONPATH": "${env:PYTHONPATH}:${workspaceFolder}"
            },
            "request": "launch",
            "program": "${workspaceFolder}/real/collision_detection/sdf_collision.py",
            "python": "/home/kiriyamagk/anaconda3/envs/cns/bin/python",  // 替换为你的实际路径
            "console": "integratedTerminal"
        }
    ]
}